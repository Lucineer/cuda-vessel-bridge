[package]
name = "cuda-vessel-bridge"
version = "0.1.0"
edition = "2021"
description = "Hardware Abstraction Layer — bridges sensors, actuators, and physical equipment to agent runtime"

[dependencies]
cuda-equipment = { path = "/tmp/fleet-workspace/cuda-equipment" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
