[package]
name = "cuda-vessel-bridge"
version = "0.1.0"
edition = "2021"
description = "Hardware abstraction bridge connecting software vessels to physical devices."
[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
